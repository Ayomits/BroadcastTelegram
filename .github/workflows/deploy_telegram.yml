name: Deploy telegram to prod

on:
  push:
    branches:
      - master
    paths:
      - telegram/**
      - shared/**
      - models/**
      - queue/**
      - repositories/**
      - .github/workflows/deploy_telegram.yml
      - docker/**

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitLab Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.gitlab.com
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build and push bot image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/telegram/Dockerfile
          push: true
          tags: |
            registry.gitlab.com/ayomits/messageretranslator:telegram_latest
            registry.gitlab.com/ayomits/messageretranslator:telegram
          build-args:
            APP_ENV=prod
          no-cache: true
          
      - name: Trigger Dokploy Deployment
        run: |
          curl -X 'POST' \
          'http://31.13.208.85:3000/api/trpc/application.deploy' \
          -H 'accept: application/json' \
          -H "x-api-key: ${{ secrets.DOKPLOY_TOKEN }}" \
          -H 'Content-Type: application/json' \
          -d '{
              "json":{
                  "applicationId": "no5eC15UpBae_zxz0A-tc"
              }
          }'